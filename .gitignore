node_modules/

# Secrets / local-only — never commit (repo is public, main auto-promotes to prod)
.env
.env.local
.env*.local
.vercel

# Research / private workspace — never belongs in this repo (see CLAUDE.md).
# Belt-and-braces: the rule is in CLAUDE.md, these patterns catch the common shapes.
scripts/research/
research/
notes/
handoffs/
*.local.md
data/
*.csv
